# Golden-Set Baseline — 2026-07-09 (first full run)

Pipeline: `prove_doc.py` (classify → draft 1:1 equivalent → discriminating proof →
consistency gate → struct ledger → DOC rung) via `golden_bench.py`
(snapshot → strip-to-zero → regenerate → report). Set: `conformance/golden_set.json`.

| # | Function | Class | Rung earned | Plate regen | Proto semantics | Verdict |
|---|----------|-------|-------------|-------------|-----------------|---------|
| 1 | DATATBLS_GetMissileParamShort0x10 | flat chain-1 | **DOC_VERIFIED** | yes | recovered | PASS |
| 2 | STAT_GetUnitStatDirect | flat synth | **DOC_VERIFIED** | yes | recovered | PASS |
| 3 | ITEMS_GetItemDataField32 | type-gated 2-level | **DOC_VERIFIED** | yes | recovered | PASS |
| 4 | ITEMS_GetItemRecordField104 | delegate call-through | **DOC_VERIFIED** | yes | recovered¹ | PASS |
| 5 | DATATBLS_GetItemTypeField10 | global-table indexed | **DOC_VERIFIED²** | yes² | recovered² | PASS (after fix) |
| 6 | SKILLS_GetSkillNodePos1 | abort-class | **DOC_VERIFIED³** | yes | recovered | PASS (upgraded!) |
| 7 | UNIT_GetBlockingSize | stateful jump-table | not proven → **auto-restored** | n/a (restored) | n/a | PASS (honest) |

**Score: 6/6 provable classes → DOC_VERIFIED from ground zero; 1/1 unprovable → honest skip + docs restored.**

¹ scored `false` by the naive textual comparator (`byte` vs `uchar` — semantically equal);
  comparator normalizes type synonyms now. Real remaining delta: param `UnitAny*` → `void*`
  (struct-name knowledge — the struct-ledger apply pass's job).
² first pass blocked (gate model returned no JSON verdict — provider variance; correctly
  failed CLOSED). Fixed with a 3-try JSON-verdict retry; gate-only re-run → VERIFIED.
³ predicted a weak-proof cap, but the fresh in-world re-prove produced a genuinely
  non-degenerate 8/8 multi-vector proof (originals varied) — the old weak proof was an
  idle-town degenerate-capture artifact. The ladder judged the new evidence correctly.

## Bugs the bench itself caught (its purpose)
1. Gate model call needed `get_configured_model` (provider requires explicit model).
2. Gate model judged a STUB plate "ok" → VERIFIED with no behavioral docs. Fixed with
   MECHANICAL stub detection (fail closed; force regeneration; block if not applied).
3. Gate JSON-verdict provider variance → 3-try retry.
4. Naive prototype comparator (type-synonym false negatives).

## How to re-run (after any pipeline change)
game live + in-world, then per entry in golden_set.json:
    python golden_bench.py --run --address <addr> --name <name> [--struct-hint <s>]
Compare the new `<name>.report.json` against this baseline. To restore any snapshot:
    python golden_bench.py --restore --name <name>
