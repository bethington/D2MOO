// SGD2FreeRes.cpp -- placeholder translation unit.
//
// This module has no reimplementation source in D2MOO. The target exists to
// satisfy CMake's add_library(SHARED ...) and to give SGD2FreeRes.patch.cpp
// (D2.Detours.patches/1.13c/) somewhere to link into. Mirrors D2Client's
// carrier exactly.
//
// Reimplementations for this module live in
// conformance/reimpl_provider/candidates/ and are bound BY NAME into the
// provider DLL at runtime -- they are not linked here.
